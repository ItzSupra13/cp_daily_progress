#include <iostream>
#include <vector>
using namespace std;
 
int main()
{
 
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        vector<int> sides(n);
        for (int side = 1; side <= n; side++) cin >> sides[side];

    }
}