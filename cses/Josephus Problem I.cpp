#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;


void solve() {
    long n;
    cin >> n;
    for(int i=1;i<=n;i++)
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    solve();
    return 0;
}